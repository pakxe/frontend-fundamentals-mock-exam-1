export type SavingProductFilterParams = {
  targetAmount: string;
  monthlyAmount: string;
  terms: number;
};

export type SavingProductFilterValue = {
  targetAmount: string;
  monthlyAmount: string;
  terms: number;
};
